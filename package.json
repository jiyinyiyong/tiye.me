{
  "packageManager": "yarn@4.12.0",
  "dependencies": {
    "@calcit/procs": "0.18.1",
    "feather-icons": "^4.29.2"
  },
  "devDependencies": {
    "bottom-tip": "^0.1.5",
    "vite": "^7.3.5"
  },
  "version": "0.0.2",
  "license": "MIT",
  "scripts": {
    "dev": "vite",
    "compile": "calcit calcit.cirru --compat-types js",
    "check": "yarn check:typed && calcit calcit.cirru --compat-types --check-only && calcit calcit.cirru analyze deprecated && calcit calcit.cirru analyze quality --baseline config/calcit-quality.cirru",
    "test": "node scripts/check-runtime-smoke.mjs",
    "build": "yarn compile && yarn test && vite build --base=./",
    "check:typed": "calcit calcit.cirru --check-only --init-fn app.updater/updater --reload-fn app.schema/normalize-action"
  }
}
